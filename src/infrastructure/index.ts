/**
 * Layer 4: Infrastructure
 *
 * Provides foundational services:
 * - LLM Gateway (Claude API integration)
 * - MCP Tool Registry
 * - Tool Sandbox (V8 Isolate / Docker)
 * - Message Broker (RabbitMQ)
 * - Database Service (PostgreSQL + Redis)
 */
