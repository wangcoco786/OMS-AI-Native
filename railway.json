{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && cd src/frontend && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "node dist/backend/server.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
